@page "/mqtt"
@inject MqttState MqttState

<MqttConnect />

@if (MqttState.IsConnected)
{
    <MqttPublish />
    <MqttSubscribe />
}


@code {
    protected override void OnInit() 
    {
        MqttState.StateChanged += (o,s) => base.Invoke(StateHasChanged);
    }
}